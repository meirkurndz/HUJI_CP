#include <iostream>
#include <algorithm>
#include <vector>
#include <stack>

using namespace std;

typedef long long ll;
typedef vector<ll> vll; 

int largest_rect(vll hist){
    hist.push_back(0);

    stack<ll> S;
    ll max_area = 0;

}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int T;
    cin >> T;

    while(--T){



    }

}